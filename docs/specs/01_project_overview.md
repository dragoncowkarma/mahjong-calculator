# 01. 제품 요구사항 정의서 (PRD): Mahjong Calculator

이 문서는 Mahjong Calculator 멀티 모듈 시스템의 핵심 사양을 정의합니다. AI 코딩 에이전트가 아키텍처의 일관성을 유지하고 로직 파편화를 방지하기 위한 주요 컨텍스트 역할을 합니다.

## 1. 프로젝트 개요 및 전략적 방향

| 항목 | 상세 설명 |
| :--- | :--- |
| **프로젝트명** | Mahjong Vision Calculator (mahjong-calculator) |
| **핵심 목표** | 리치 마작(Riichi Mahjong)의 실시간 득점 계산 및 의사결정을 보조하는 사용자 대면 프론트엔드 애플리케이션을 구축하고, 동시에 마작 인공지능 강화를 위한 대규모 시뮬레이션 및 학습 데이터 텐서 변환 파이프라인을 구축한다. |
| **비전** | 마작의 복잡한 점수 계산을 단 한 번의 촬영으로 해결하여, 모든 플레이어가 계산의 번거로움 없이 서비스의 본질적인 재미에만 집중할 수 있는 환경을 제공한다. |
| **아키텍처 전략** | 모노레포(Monorepo) 구조 하에서 공통 도메인 로직(Shared Core)을 공유하되, **애플리케이션 모듈 (KMP)**과 **데이터 생성 모듈 (Python)**을 느슨하게 결합하여 독립적인 빌드 및 배포가 가능하도록 분리한다. |

## 2. 고유 가치 제안 (Unique Value Proposition)

> **"찍으면 끝. 마작 점수 계산, 이제 카메라 한 번이면 됩니다."**

- 기존 마작 점수 계산 앱은 패를 수동으로 입력해야 하는 번거로움이 있음
- 본 앱은 **카메라 인식 + AI 자동 판별**로 입력 과정을 혁신적으로 단축
- **완전한 한국어 지원**으로 국내 마작 유저에게 최적화된 경험 제공
- **오프라인 완전 동작**으로 네트워크 환경에 무관하게 사용 가능

## 3. 타겟 사용자

### 3.1. 앱 사용자 (외부)
| 세그먼트 | 설명 | 핵심 니즈 |
| :--- | :--- | :--- |
| **입문자** | 점수 계산법을 익히지 못해 원활한 진행에 어려움을 겪는 사용자 | 빠르고 정확한 자동 계산, 역 학습 |
| **오프라인 플레이어** | 자동 작탁이 없는 환경에서 정산을 원하는 사용자 | 신뢰할 수 있는 즉시 정산 |
| **숙련자** | 복잡한 상황에서의 검증 도구로 활용하려는 사용자 | 엣지 케이스 정확도, 속도 |

### 3.2. 내부 사용자
| 세그먼트 | 설명 |
| :--- | :--- |
| **AI/ML 개발자** | 강화학습 기반 마작 AI 연구를 위한 학습 데이터 파이프라인 활용 (`ml-pipeline/`) |

## 4. 경쟁 분석

| 앱/서비스 | 플랫폼 | 인식 방식 | 한국어 | 차별점 대비 약점 |
| :--- | :--- | :--- | :--- | :--- |
| 麻雀カメラ | iOS | 카메라 인식 | ✗ | 일본어 전용, 정확도 불안정 |
| Mahjong Helper | Android/iOS | 수동 입력 | ✗ | 수동 입력만 지원 |
| 마작 점수 계산기 (국내) | Android | 수동 입력 | ✓ | 카메라 인식 미지원 |
| **본 앱 (계획)** | **Android/iOS** | **카메라 AI + 수동 교정** | **✓** | **카메라 인식 + 한국어 + 크로스플랫폼** |

## 5. 비즈니스 모델 (v2.0 이후)

| 모델 | 설명 |
| :--- | :--- |
| **기본 (무료)** | 핵심 기능: 카메라 인식, 점수 계산, 역 목록 |
| **프리미엄 (일회성 구매)** | 고급 통계, 세션 히스토리 무제한, 타패 가이드(샨텐 분석) |
| **론칭 전략** | MVP는 전체 무료 → 사용자 기반 확보 후 프리미엄 기능 분리 |

## 6. 모듈 정의

### 6.1. 애플리케이션 모듈 (app/)
- **목적**: 실시간 사용자 상호작용 및 상태 비저장(Stateless) 점수 계산.
- **플랫폼**: Compose Multiplatform을 통한 iOS (CoreML) 및 Android (TFLite).
- **핵심 지표**: 계산 지연 시간(50ms 미만) 및 UI 반응성.

### 6.2. 학습 데이터 생성 모듈 (ml-pipeline/)
- **목적**: 강화학습을 위한 고처리량 시뮬레이션 및 상태 텐서화.
- **플랫폼**: Python (PyTorch, Albumentations).
- **핵심 지표**: 처리량 (분당 수천 게임) 및 데이터 정확도.

## 7. 론칭 로드맵

| 단계 | 시장 | 언어 | 비고 |
| :--- | :--- | :--- | :--- |
| **MVP (v1.0)** | 한국 | 한국어 | 핵심 기능 검증, 사용자 피드백 수집 |
| **v1.5** | 한국 + 일본 | 한국어, 일본어 | 일본어 지원 추가, 일본 마작 커뮤니티 타겟 |
| **v2.0** | 글로벌 | + 영어 | 프리미엄 기능, ASO 최적화 |
